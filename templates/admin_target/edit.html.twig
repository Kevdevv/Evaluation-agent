{% extends 'base.html.twig' %}

{% block title %}Modifier la Cible{% endblock %}

{% block body %}
    <h1>Modifier la Cible</h1>
<div class="container">
    
    {{ form_start(form) }}
        <div>{{ form_rest (form)}}</div>
        <div class="d-flex justify-content-evenly">
            <button class="btn btn-primary mb-3">{{ button_label|default('Mettre à jour') }}</button>
            <a class="btn btn-secondary mb-3" href="{{ path('app_admin_target_index') }}">Retour</a>
            {{ include('admin_target/_delete_form.html.twig') }}
        </div>
    {{ form_end(form) }}
    
</div>
{% endblock %}
