<form method="post" action="{{ path('app_admin_target_delete', {'id': target.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette cible ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ target.id) }}">
    <button class="btn btn-danger mb-3">Supprimer</button>
</form>
