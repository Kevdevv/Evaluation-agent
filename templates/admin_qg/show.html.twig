{% extends 'base.html.twig' %}

{% block title %}Qg{% endblock %}

{% block body %}
    <h1>Qg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qg.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ qg.code }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ qg.address }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ qg.country }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ qg.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_qg_index') }}">back to list</a>

    <a href="{{ path('app_admin_qg_edit', {'id': qg.id}) }}">edit</a>

    {{ include('admin_qg/_delete_form.html.twig') }}
{% endblock %}
